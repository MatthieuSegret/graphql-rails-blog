mutation changePassword($password: String, $password_confirmation: String, $current_password: String) {
  changePassword(
    input: { password: $password, password_confirmation: $password_confirmation, current_password: $current_password }
  ) {
    messages {
      field
      message
    }
  }
}
